Assembly png
Type static_lib

if cl Compiler cl else Compiler gcc

if release  BuildDirectory Build/Release
if !release BuildDirectory Build/Debug

IncludeFlags .
Includes .

Libraries zlib
LibraryDirectories Build/zlib
